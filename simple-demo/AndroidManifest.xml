<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.lx.yht"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/myAppTheme">
        <!--myAppTheme 主题继承自SDKAppTheme-->

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ConfirmActivity"/>
        <activity android:name=".ContractListActivity"/>
        <activity android:name=".ConfirmPreviewActivity"/>

        <!--注册从云合同开发者平台注册的AppId-->
        <meta-data
            android:name="YhtSdk_AppId"
            android:value="id_请到云合同开发平台申请appId"/>
    </application>

</manifest>